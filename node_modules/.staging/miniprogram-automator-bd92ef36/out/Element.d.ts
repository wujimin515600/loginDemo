import Connection from './Connection';
interface IElementOptions {
    id: string;
    pageId: number;
    tagName: string;
}
export default class Element {
    tagName: string;
    private connection;
    private id;
    private pageId;
    private elementMap;
    constructor(connection: Connection, options: IElementOptions, elementMap: Map<string, Element>);
    $(selector: string): Promise<Element | null | undefined>;
    $$(selector: string): Promise<any>;
    size(): Promise<{
        width: string;
        height: string;
    }>;
    offset(): Promise<any>;
    text(): Promise<string | string[]>;
    attribute(name: string): Promise<string | string[]>;
    wxml(): Promise<any>;
    outerWxml(): Promise<any>;
    value(): Promise<string | string[]>;
    style(name: string): Promise<string | string[]>;
    tap(): Promise<void>;
    longpress(): Promise<void>;
    trigger(type: string, detail?: any): Promise<void>;
    private touchstart;
    private touchend;
    private domProperty;
    private property;
    private getter;
    private send;
    static create(connection: Connection, options: IElementOptions, elementMap: Map<string, Element>): Element | undefined;
}
export {};
