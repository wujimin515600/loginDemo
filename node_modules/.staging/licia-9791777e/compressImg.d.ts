import isFn = require('./isFn');
import loadImg = require('./loadImg');
import noop = require('./noop');
import defaults = require('./defaults');
import createUrl = require('./createUrl');
import isStr = require('./isStr');

declare namespace compressImg {
    interface IOptions {
        maxWidth?: number;
        maxHeight?: number;
        width?: number;
        height?: number;
        mimeType?: string;
        quality?: number;
    }
}
declare function compressImg(file: File | Blob | string, cb: Function): void;
declare function compressImg(
    file: File | Blob | string,
    opts?: compressImg.IOptions,
    cb?: Function
): void;

export = compressImg;
