import escapeJsStr = require('./escapeJsStr');
import type = require('./type');
import toStr = require('./toStr');
import endWith = require('./endWith');
import toSrc = require('./toSrc');
import keys = require('./keys');
import each = require('./each');
import Class = require('./Class');
import getProto = require('./getProto');
import difference = require('./difference');
import extend = require('./extend');
import isPromise = require('./isPromise');
import filter = require('./filter');
import now = require('./now');
import allKeys = require('./allKeys');
import contain = require('./contain');

declare namespace stringifyAll {
    interface IOptions {
        unenumerable?: boolean;
        symbol?: boolean;
        accessGetter?: boolean;
        timeout?: number;
        depth?: number;
        ignore?: any[];
    }
}
declare function stringifyAll(
    obj: any,
    options?: stringifyAll.IOptions
): string;

export = stringifyAll;
