import last = require('./last');
import arrToMap = require('./arrToMap');
import startWith = require('./startWith');
import lowerCase = require('./lowerCase');

declare namespace parseHtml {
    interface IHandlers {
        start?: (tag: string, attrs: any, unary: boolean) => void;
        end?: (tag: string) => void;
        comment?: (text: string) => void;
        text?: (text: string) => void;
    }
}
declare function parseHtml(html: string, handlers: parseHtml.IHandlers): void;

export = parseHtml;
