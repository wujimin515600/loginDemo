import isStr = require('./isStr');
import base64 = require('./base64');
import isArrBuffer = require('./isArrBuffer');
import isArr = require('./isArr');
import isBuffer = require('./isBuffer');
import type = require('./type');
import lowerCase = require('./lowerCase');

declare namespace convertBin {
    function blobToArrBuffer(blob: any): Promise<ArrayBuffer>;
}
declare function convertBin(bin: any, type: string): any;

export = convertBin;
