import utf8 = require('./utf8');
import base64 = require('./base64');
import bytesToStr = require('./bytesToStr');
import strToBytes = require('./strToBytes');

declare const rc4: {
    encrypt(key: string, str: string): string;
    decrypt(key: string, str: string): string;
};

export = rc4;
