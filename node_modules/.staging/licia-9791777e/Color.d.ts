import Class = require('./Class');
import isStr = require('./isStr');
import clamp = require('./clamp');
import rgbToHsl = require('./rgbToHsl');
import hslToRgb = require('./hslToRgb');
import lpad = require('./lpad');
import convertBase = require('./convertBase');

declare namespace Color {
    interface IColor {
        val: number[];
        model: string;
    }
}
declare class Color {
    constructor(color: string | Color.IColor);
    toRgb(): string;
    toHex(): string;
    toHsl(): string;
    static parse(colorStr: string): Color.IColor;
}

export = Color;
