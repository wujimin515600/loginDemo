import isNode = require('./isNode');
import root = require('./root');
import memStorage = require('./memStorage');

declare function deprecate(fn: Function, msg: string): Function;

export = deprecate;
