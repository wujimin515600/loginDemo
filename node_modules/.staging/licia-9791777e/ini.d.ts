import each = require('./each');
import trim = require('./trim');
import safeSet = require('./safeSet');
import safeGet = require('./safeGet');
import endWith = require('./endWith');
import isArr = require('./isArr');
import isObj = require('./isObj');

declare namespace ini {
    interface IOptions {
        section?: string;
        whitespace: boolean;
    }
}
declare const ini: {
    parse(ini: string): any;
    stringify(obj: any, options?: ini.IOptions): string;
};

export = ini;
