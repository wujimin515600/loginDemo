import loadJs = require('./loadJs');
import defaults = require('./defaults');
import noop = require('./noop');
import uniqId = require('./uniqId');
import query = require('./query');

declare namespace jsonp {
    interface IOptions {
        url: string;
        data?: any;
        success?: Function;
        param?: string;
        name?: string;
        error?: Function;
        complete?: Function;
        timeout?: number;
    }
}
declare function jsonp(opts: jsonp.IOptions): void;

export = jsonp;
