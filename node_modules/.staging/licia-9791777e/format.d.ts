import restArgs = require('./restArgs');
import toInt = require('./toInt');
import toNum = require('./toNum');
import toStr = require('./toStr');

declare function format(str: string, ...values: any[]): string;

export = format;
