import isBrowser = require('./isBrowser');
import toInt = require('./toInt');
import keys = require('./keys');

declare namespace detectBrowser {
    interface IBrowser {
        name: string;
        version: number;
    }
}
declare function detectBrowser(ua?: string): detectBrowser.IBrowser;

export = detectBrowser;
