import parseHtml = require('./parseHtml');
import Stack = require('./Stack');
import isArr = require('./isArr');
import each = require('./each');
import isStr = require('./isStr');
import mapObj = require('./mapObj');

declare const html: {
    parse(html: string): any;
    stringify(tree: any[]): string;
};

export = html;
