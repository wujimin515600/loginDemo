import isStr = require('./isStr');
import isDate = require('./isDate');
import toStr = require('./toStr');
import lpad = require('./lpad');

declare function dateFormat(
    date: Date,
    mask: string,
    utc?: boolean,
    gmt?: boolean
): string;
declare function dateFormat(mask: string, utc?: boolean, gmt?: boolean): string;

export = dateFormat;
