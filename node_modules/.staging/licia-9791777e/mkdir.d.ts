import isFn = require('./isFn');
import noop = require('./noop');

declare function mkdir(dir: string, mode?: number, cb?: Function): void;
declare function mkdir(dir: string, cb?: Function): void;

export = mkdir;
