import type = require('./type');
import isStr = require('./isStr');
import utf8 = require('./utf8');
import strToBytes = require('./strToBytes');

declare function crc1(
    input: string | Buffer | ArrayBuffer | Uint8Array,
    previous?: number
): number;

export = crc1;
