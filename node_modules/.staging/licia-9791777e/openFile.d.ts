declare namespace openFile {
    interface IOptions {
        accept?: string;
        multiple?: boolean;
    }
}

declare function openFile(options?: openFile.IOptions): Promise<File[]>;

export = openFile;
