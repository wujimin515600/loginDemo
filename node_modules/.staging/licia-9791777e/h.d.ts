import isEl = require('./isEl');
import isStr = require('./isStr');
import startWith = require('./startWith');
import $class = require('./$class');
import $css = require('./$css');
import each = require('./each');
import isFn = require('./isFn');

declare function h(
    tag: string,
    attrs?: { [name: string]: any },
    ...child: Array<string | HTMLElement>
): HTMLElement;

export = h;
