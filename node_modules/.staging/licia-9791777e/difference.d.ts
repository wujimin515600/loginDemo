import restArgs = require('./restArgs');
import flatten = require('./flatten');
import filter = require('./filter');
import contain = require('./contain');

declare function difference(arr: any[], ...rest: any[]): any[];

export = difference;
