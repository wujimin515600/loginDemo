import isStr = require('./isStr');
import isObj = require('./isObj');
import kebabCase = require('./kebabCase');
import isUndef = require('./isUndef');
import contain = require('./contain');
import isNum = require('./isNum');
import $safeEls = require('./$safeEls');
import prefix = require('./prefix');
import each = require('./each');

declare function $css(element: $safeEls.El, name: string): string;
declare function $css(element: $safeEls.El, name: string, value: string): void;
declare function $css(
    element: $safeEls.El,
    properties: { [name: string]: string }
): void;

export = $css;
