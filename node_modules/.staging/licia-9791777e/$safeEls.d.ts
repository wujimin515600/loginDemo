import isStr = require('./isStr');
import toArr = require('./toArr');
import Select = require('./Select');

declare namespace $safeEls {
    type El = Element | Element[] | NodeListOf<Element> | string;
}
declare function $safeEls(value: $safeEls.El): Element[];

export = $safeEls;
