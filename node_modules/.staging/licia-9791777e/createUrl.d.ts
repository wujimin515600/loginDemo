import defaults = require('./defaults');
import isBlob = require('./isBlob');
import isFile = require('./isFile');
import Blob = require('./Blob');
import toArr = require('./toArr');

declare namespace createUrl {
    interface IOptions {
        type?: string;
    }
}
declare function createUrl(data: any, options?: createUrl.IOptions): string;

export = createUrl;
