import noop = require('./noop');
import nextTick = require('./nextTick');
import restArgs = require('./restArgs');

declare function waterfall(tasks: Function[], cb?: Function): void;

export = waterfall;
