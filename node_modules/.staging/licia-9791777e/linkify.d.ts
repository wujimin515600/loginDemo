import extractUrls = require('./extractUrls');
import each = require('./each');
import escapeRegExp = require('./escapeRegExp');

declare function linkify(str: string, hyperlink?: Function): string;

export = linkify;
