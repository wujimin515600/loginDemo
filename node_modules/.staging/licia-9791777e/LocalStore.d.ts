import Store = require('./Store');
import safeStorage = require('./safeStorage');
import isEmpty = require('./isEmpty');
import stringify = require('./stringify');
import defaults = require('./defaults');
import isObj = require('./isObj');

declare class LocalStore extends Store {
    constructor(name: string, data?: {});
}

export = LocalStore;
