import escape = require('./escape');
import defaults = require('./defaults');
import escapeJsStr = require('./escapeJsStr');

declare function template(str: string, util?: any): Function;

export = template;
