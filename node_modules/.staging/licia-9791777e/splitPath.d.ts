declare namespace splitPath {
    interface IPath {
        dir: string;
        name: string;
        ext: string;
    }
}
declare function splitPath(path: string): splitPath.IPath;

export = splitPath;
