import isFn = require('./isFn');
import noop = require('./noop');
import defaults = require('./defaults');
import isObj = require('./isObj');
import query = require('./query');

declare namespace ajax {
    interface IOptions {
        url: string;
        data?: string | {};
        dataType?: string;
        contentType?: string;
        success?: Function;
        error?: Function;
        complete?: Function;
        timeout?: number;
    }
    function get(
        url: string,
        data: any,
        success: Function,
        dataType?: string
    ): XMLHttpRequest;
    function post(
        url: string,
        data: any,
        success: Function,
        dataType?: string
    ): XMLHttpRequest;
}
declare function ajax(options: ajax.IOptions): XMLHttpRequest;

export = ajax;
