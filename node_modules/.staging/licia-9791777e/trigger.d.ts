import isStr = require('./isStr');
import defaults = require('./defaults');
import extend = require('./extend');

declare function trigger(el: Element, type: string, options?: any);
declare function trigger(type: string, options?: any);

export = trigger;
